{
  "inclusionPriority": {
    "title": "Inclusion priority",
    "description": "Inclusion priority in relation to other autocomplete-plus providers",
    "type": "integer",
    "default": 20,
    "order": 0
  },
  "excludeLowerPriority": {
    "title": "Exclude lower priority providers",
    "description": "Whether to exclude lower priority providers",
    "type": "boolean",
    "default": false,
    "order": 1
  },
  "loadClassMembers": {
    "title": "Load class members",
    "description": "If enabled, you can examine class methods and other members e.g. 'ArrayList.contains'.  If you have enabled the 'load class members' setting, I recommend you to put the most important paths in the classpath first, because class descriptions are loaded in the order specified in the .classpath file.",
    "type": "boolean",
    "default": true,
    "order": 2
  },
  "organizeImportsOnSave": {
    "title": "Organize imports on save",
    "description": "",
    "type": "boolean",
    "default": false,
    "order": 3
  },
  "classpathFilePath": {
    "title": "Classpath file path",
    "description": "If you bump into conflicts with other tools, e.g. Eclipse that uses .classpath in XML format, use this to rename the .classpath file to .classpath-atom or something else.",
    "type": "string",
    "default": ".classpath",
    "order": 4
  }
}
